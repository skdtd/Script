# 直播间列表(就是那个pid, 有很多个用英文逗号隔开)
PIDS=1199613785796,

# 指定主播的名称(有很多个用英文逗号隔开)
特定主播=冰者金網,李四

# 价格界限
指定价格=5000

# 三种比例
高比例=0.68
低比例=0.64
非特定用户比例=0.46

# 采集过程中打开浏览器看一下采集过程(没啥好看的)
是否打开浏览器=否

# 多个直播间是否同时开始采集, 否则将会按顺序进行采集(没事就开着, 除非很卡)
是否同时采集=是

# 程序会根据'定时采集间隔'设置的时间间隔在一次采集结束后自动等待下一次采集(程序会一直开着, 过段时间采一次)
是否开启定时采集=否

# 每次采集的间隔时间, 从上一次结束到下一次开始的间隔时间, 需要将'是否开启定时采集'设置为是才能生效, 单位: 秒
# 5分钟:  300
# 10分钟: 600
# 30分钟: 1800
# 1小时:  3600
# 24小时: 86400
定时采集间隔=5

# 生成文件到目标文件夹, 文件夹不存在会自动创建, 如果不能创建文件夹直接退出程序
输出目录=./采集/