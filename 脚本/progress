#!/bin/absh

TOTAL=50

SPEED=$(( $1 * ${TOTAL} / 100 ))
TEXT=$2

[[ ${SPEED} -ge ${TOTAL} ]] && SPEED=${TOTAL} && TEXT='Completion!\n'

L=$(perl -E "say'=' x ${SPEED}")
R=$(perl -E "say' ' x $((${TOTAL} - ${SPEED}))")
echo -ne "\r[${L}${R}]${TEXT}"